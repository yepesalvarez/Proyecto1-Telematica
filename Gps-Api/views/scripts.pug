script.
    function initMap() 
    {
        alert("let's begin!");
        var directionsService = new google.maps.DirectionsService;
        var directionsDisplay = new google.maps.DirectionsRenderer;
        var map = new google.maps.Map(document.getElementById('map'),
        {
            zoom: 6,
            center: {lat: 41.85, lng: -87.65}
        });
        directionsDisplay.setMap(map);
    }
    function calculateAndDisplayRoute(points) 
    {
        alert('calculate and display');
        var directionsService = new google.maps.DirectionsService;
        var directionsDisplay = new google.maps.DirectionsRenderer;
        var waypts = [];
        for (var i = 0; i < points.length; i++) {
            waypts.push({
            location: points[i].value,
            stopover: true
            }
        }
        directionsService.route({
            origin: points[0].value,
            destination: points[length-1].value,
            waypoints: waypts,
            optimizeWaypoints: true,
            travelMode: 'DRIVING'
        }, function(response, status) {
            if (status === 'OK') {
                directionsDisplay.setDirections(response);
                var route = response.routes[0];
            } else {
             window.alert('Directions request failed due to ' + status);
            }
        });
    }
    function saludar(){
        alert('holi')
    }
    script(async='', defer='', src='https://maps.googleapis.com/maps/api/js?key=AIzaSyAFbduH0tacZ0T_X1XgNBRD9uftgKMxhek&callback=initMap')